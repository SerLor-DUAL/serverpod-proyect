class: UsersRegistry
table: users_registry
fields:
  userName: String
  userPassword: PasswordGenerator?, relation=parent # Foreign key field
indexes:
  user_name_idx:
    fields: userName
    unique: true