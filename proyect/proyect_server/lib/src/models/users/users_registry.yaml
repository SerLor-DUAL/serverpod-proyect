class: UsersRegistry
table: users_registry
fields:
  userName: String
  userPassword: String
  options: int, relation(parent=password_options)
indexes:
  user_name_idx:
    fields: userName
    unique: true